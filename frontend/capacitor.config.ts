import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yirmisaniye.app',
  appName: 'Yirmi Saniye',
  webDir: 'www/browser'
};

export default config;
